---
import Logo from '@/components/Logo.astro';
import '@/styles/global.css';

const links = [
  { key: 'our-story', link: '/our-story', label: 'Our Story' },
  // { key: 'gallery', link: '/gallery', label: 'Gallery' },
  { key: 'faq', link: '/faq', label: 'FAQ' },
  // { key: 'blog', link: '/blog', label: 'Blog' },
];

const currentPath = Astro.url.pathname;
---

<header class="h-20">
  <div class="container mx-auto flex h-full max-w-7xl items-center px-6 lg:px-12">
    <div class="flex w-full items-center justify-between">
      <Logo />

      <nav class="flex items-center gap-4">
        {
          links.map((link) => (
            <a
              href={link.link}
              class:list={[
                'text-sm font-medium transition-colors hover:text-gray-900',
                currentPath === link.link ? 'text-gray-900' : 'text-gray-600',
              ]}
            >
              {link.label}
            </a>
          ))
        }
      </nav>
    </div>
  </div>
</header>
